# Everything in this file is optional. Still, the file needs to exist. Adding
# metadata here will improve the DX when using components.

# This is so your IDE knows about the syntax for fixes and autocomplete.
$schema: https://git.drupalcode.org/project/sdc/-/raw/1.x/src/metadata.schema.json
name: Base Field
description: Custom field component
status: stable
group: Fields

props:
  type: object
  properties:
    field_name:
      type: string
      title: Field Name
      description: Becomes the class of the field
    content:
      type: ["string","null"]
      title: Content
      description: Field content.
